#!/usr/bin/python3

from django.urls import path

from . import views

urlpatterns = [
#    path('', views.index, name='index'),
#    path('<int:model_obj_id>', views.single_buyer),
#    path('<int:outer_model_obj_id>/to_buy_listings', views.single_buyer_to_buy_listings),
#    path('<int:outer_model_obj_id>/to_buy_listings/<int:inner_model_obj_id>', views.single_buyer_single_to_buy_listing),
]
