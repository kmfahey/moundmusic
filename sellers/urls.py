#!/usr/bin/python3

from django.urls import path

from . import views

urlpatterns = [
#    path('', views.index, name='index'),
#    path('<int:model_obj_id>', views.single_seller),
#    path('<int:outer_model_obj_id>/to_sell_listings', views.single_seller_to_sell_listings),
#    path('<int:outer_model_obj_id>/to_sell_listings/<int:inner_model_obj_id>', views.single_seller_single_to_sell_listing),
]
